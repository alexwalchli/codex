// import * as firebaseRequestQueueActionTypes from './firebase-request-queue-action-types'

// export const queueRequest = (requestId, context, request) => ({
//   type: firebaseRequestQueueActionTypes.QUEUE_REQUEST,
//   payload: {
//     requestId,
//     context,
//     request
//   }
// })

// export const queuedRequestStarted = (requestId) => ({
//   type: firebaseRequestQueueActionTypes.QUEUED_REQUEST_STARTED,
//   payload: { requestId }
// })

// export const queuedRequestCompleted = (requestId) => ({
//   type: firebaseRequestQueueActionTypes.QUEUED_REQUEST_COMPLETED,
//   payload: { requestId }
// })

// export const queuedRequestFailed = (requestId, error) => ({
//   type: firebaseRequestQueueActionTypes.QUEUED_REQUEST_FAILED,
//   payload: { requestId, error }
// })
